# Введення значень a, b, c з клавіатури
a = 7
b = -5
c = 0

# Підрахунок кількості додатних чисел
count_positive = 0
if a > 0:
    count_positive += 1
if b > 0:
    count_positive += 1
if c > 0:
    count_positive += 1

# Виведення результату
print("Кількість додатних чисел серед a, b, c:", count_positive)
