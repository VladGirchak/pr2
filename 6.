# Конкретні значення цілих чисел a та b
a = 8
b = 5

# Перевірка, чи числа рівні
if a == b:
    a = b = 0
else:
    max_number = max(a, b)
    a = b = max_number

# Виведення оновлених значень чисел
print("a =", a)
print("b =", b)
