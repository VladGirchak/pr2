# Увід конкретних значень x і y
x = 3
y = 6

# Перевірка, чи числа не рівні одне одному
if x != y:
    # Заміна чисел відповідно до умови завдання
    if x < y:
        x = (x + y) / 2
        y = 2 * x
    else:
        y = (x + y) / 2
        x = 2 * y

    # Вивід оновлених значень
    print("Після заміни:")
    print("x =", x)
    print("y =", y)
else:
    print("Числа x і y повинні бути різними.")

