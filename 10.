# Конкретні числа a, b, c, k
a = 9
b = 12
c = 15
k = 3

# Перевірка, чи число k є дільником a, b, c
if a % k == 0:
    print(f"{k} є дільником числа a.")
if b % k == 0:
    print(f"{k} є дільником числа b.")
if c % k == 0:
    print(f"{k} є дільником числа c.")
